<?xml version="1.0" encoding="UTF-8"?>
<configuration>
<!-- Insert the current time formatted as "yyyyMMdd'T'HHmmss" under
       the key "bySecond" into the logger context. This value will be
       available to all subsequent configuration elements. -->
  <timestamp key="bySecond" datePattern="yyyyMMdd'T'HHmmss"/>

  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <Pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</Pattern>
      <!--<Pattern>[%d{dd/MM/yyyy HH:mm:ss.SSS}] [%level] [%thread] %logger{36} - %msg %n</Pattern> -->
    </encoder>
  </appender>

  <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>sapclient-${bySecond}.log</file>
        <append>true</append>
        <encoder>
            <pattern>[%d{dd/MM/yyyy HH:mm:ss.SSS}] [%level] [%thread] %logger{36} - %msg %n</pattern>
        </encoder>
    </appender>
  <!--
  <logger name="com.base22" level="TRACE"/>
 -->
<!--
  <logger name="ch.deepimpact.tamara.sapclient" level="INFO"/>

  <logger name="org.apache.cxf" level="INFO"/>
  -->
    <logger name="ch.deepimpact.tamara.sapclient.json" level="DEBUG"/>
  <!--
  <logger name="ch.deepimpact" level="INFO"/>
  -->
  <root level="DEBUG">

    <appender-ref ref="STDOUT"/>

    <appender-ref ref="FILE"/>
      <!--
    -->
  </root>
</configuration>
